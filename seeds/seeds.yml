version: 2

seeds:
  
  - name: states
    description: List of the american states and their shortcuts
    columns:
      - name: State_ID
        description: Unique id for each state.
#        tests:
#          - not_null
#          - unique

  - name: calendar_short
    description: modified csv file with the necessary date field
    columns:
      - name: date_num
        description: Unique id for each day.